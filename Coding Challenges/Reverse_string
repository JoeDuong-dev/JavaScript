// This function reverses the string that is passed to it.

// Using join('') to remove the comma separating the string.
function reverseString(str){
  return str.split('').reverse().join('');  
}   

console.log(reverseString("Hello World"))